diff --git a/train.py b/train.py
index 39c740d..57269dc 100644
--- a/train.py
+++ b/train.py
@@ -251,4 +251,23 @@ X, Y = get_batch('train') # fetch the very first batch
 t0 = time.time()
 local_iter_num = 0 # number of iterations in the lifetime of this process
 raw_model = model.module if ddp else model # unwrap DDP container if needed
+
+def save_checkpoint():
+    checkpoint = {
+        'model': raw_model.state_dict(),
+        'optimizer': optimizer.state_dict(),
+        'model_args': model_args,
+        'iter_num': iter_num,
+        'best_val_loss': best_val_loss,
+        'config': config,
+    }
+    checkpoint_path = os.path.join(out_dir, 'ckpt.pt')
+    temporary_path = os.path.join(out_dir, f'.ckpt.pt.tmp.{os.getpid()}')
+    try:
+        torch.save(checkpoint, temporary_path)
+        os.replace(temporary_path, checkpoint_path)
+    finally:
+        if os.path.exists(temporary_path):
+            os.unlink(temporary_path)
+
 running_mfu = -1.0
@@ -274,15 +293,7 @@ while True:
         if losses['val'] < best_val_loss or always_save_checkpoint:
             best_val_loss = losses['val']
             if iter_num > 0:
-                checkpoint = {
-                    'model': raw_model.state_dict(),
-                    'optimizer': optimizer.state_dict(),
-                    'model_args': model_args,
-                    'iter_num': iter_num,
-                    'best_val_loss': best_val_loss,
-                    'config': config,
-                }
                 print(f"saving checkpoint to {out_dir}")
-                torch.save(checkpoint, os.path.join(out_dir, 'ckpt.pt'))
+                save_checkpoint()
     if iter_num == 0 and eval_only:
         break
@@ -331,5 +342,9 @@ while True:
     # termination conditions
     if iter_num > max_iters:
         break
-
+
+if master_process and iter_num > 0 and not eval_only:
+    print(f"saving final checkpoint to {out_dir}")
+    save_checkpoint()
+
 if ddp:
